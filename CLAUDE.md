# CLAUDE

- Read `/context/PROJECT_MAP.md`, `/context/CURRENT_STATE.md`, `/context/ROLES.md`, and `/context/DECISIONS.md` first
- Classify the task into one role using `/context/ROLES.md`
- State `Role: <name>` before acting
- Avoid full repo scans unless ownership is unclear or the task truly spans modules
- Prefer targeted file reads from `/context/PROJECT_MAP.md`
- Before major edits, state a short plan
- Update `/context/CURRENT_STATE.md` after meaningful changes
- Respect `/context/DECISIONS.md`; if deviating, state why briefly
